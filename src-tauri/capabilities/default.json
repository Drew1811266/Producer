{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default desktop capability for Producer backend commands.",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}
